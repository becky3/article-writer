# 品質ガイド (write-hatena-diary)

はてなブログ向け日記記事の品質ルール SSoT。構造・記法・禁止情報・事実正確性など、どんな記事でも必ず守る絶対原則のみを扱う。タイトルの付け方・文体・解説・地の文など「どう書くか」は `narrative-guidelines.md` 側にある。

参照元:

- `/write-hatena-diary`: 記事生成時に参照
- `/review-hatena-diary` 観点 2「ガイドライン準拠」: 機械照合ソース
- `/multi-perspective-review` 観点 5「ガイドライン準拠チェック」: 機械照合ソース

各項目は箇条書きで端的に書く。具体的なセリフ例・言い換え例は本ファイルに置かない。

---

## 構成

- セクション順序・固定 HTML 文言は `template-diary.md` の並びに従う
- 各話題ごとに端的な H2 見出しを置き、吹き出し対話で展開する
- 1 H2 見出しにつき 1 話題
- 文体・進行・相槌・冒頭の入り方・地の文・タイトルの付け方は `narrative-guidelines.md` を参照

## タイトル一致

- フロントマター `title:` と本文先頭 H1 の文字列を一致させる

## 記法

- GitHub-flavored Markdown のみで記述する
- はてな記法は使わない
- 絵文字は Unicode 直接記述を使う
- ショートコード形式（`:sparkles:` 等）は使わない
- 吹き出しは単行マーカー（`kuro-chan>>...` / `nee-san>>...`）、Bluesky 埋め込みはフェンス（`{{{bluesky ... }}}`）で書く
- HTML タグを直接書かない
- 簡素記法の仕様は `balloon-html.md` 参照
- インラインコードは balloon の内外を問わず `` `name` `` で書く
- 本文の数字は原則アラビア数字で書く（例: `50 ファイル`・`600 件`・`5 か所`・`0.1`）。漢数字は慣用句・固有名詞・「一つ」「二人」程度の数詞などアラビア数字だと不自然な場合のみ
- 吹き出し・キャラ紹介カードの class 名・構造は `hatena-design.css` を SSoT として参照
- 記事内に `<style>` を埋め込まない
- はてなブログ側の編集モードを Markdown に設定しておく

## リポジトリ参照

- リポ名は backtick で囲んで本文に書く
- `owner/name` 形式・URL・Issue 番号・PR 番号は本文に書かない

## 登場人物セクションのマーカー置換

- `{{char-A-line}}` `{{char-B-line}}` `{{char-C-line}}` の 3 マーカーは必ずすべて置換する
- 一言の方針は `narrative-guidelines.md`「登場人物セクションの一言」を参照
- 事実情報部分（`<strong>名前</strong>` 〜 `<br>` 直前まで）は改変しない

## Bluesky 投稿の扱い

- 引用は `{{{bluesky ... }}}` 簡素記法で記述する
- 記法仕様は `balloon-html.md` 参照
- 1 投稿 = 1 ブロック
- その日の Bluesky 投稿が 0 件なら Bluesky 関連シーンを省略
- 引用ブロック直後のペルソナコメント運用・引用の前提・関係性は `narrative-guidelines.md`「Bluesky 引用部の運用」を参照

## 一次情報の重視

- 事実関係を歪めない
- ジャーナルにない情報を持ち込まない
- 推測・憶測を事実として書かない
- 未確認の内容は明示的に注記する

## 内容の正確性

- 未検証の機能・環境・実行条件を断定的に書かない
- 最上級・断定表現を避ける
- 日記の主題から逸れる情報は含めない
- 個別契約に依存するコスト・料金情報は金額を避けるか前提条件を明示する
- 異なるシーン間で前提・結論が食い違わないか確認する
- リポジトリと機能（feature）を取り違えない。リポジトリ名・機能名・役割は対象リポジトリの README / docs/specs と整合させる

## 個人・環境固有情報の混入禁止

書いてはいけないもの:

- 絶対ローカルパス
- 実セッション ID、UUID、トークン、ハッシュ等の実値
- 認証情報の値
- 実在の人物・作品由来の固有名詞、台詞、決め台詞
- GitHub アカウント名

## Ingester データの扱い

`rag-knowledge` の各 Ingester が外部サイトから取り込んだコンテンツの扱いを定める。

禁止対象:

- Ingester 経由で取り込んだ外部コンテンツへの言及（取り込み元サイトの記事タイトル・本文・固有情報・引用）
- 取り込み作業を記事の主題・素材として扱うこと（取り込み件数・成功失敗・取り込み元サイト名を主題化する記述）

許容範囲:

- Bluesky（社長 SNS）の引用は従来通り許可。詳細は `narrative-guidelines.md`「社長の SNS（Bluesky）について」を参照
- 自分発信の情報源は許可。自社・自身が公開した Zenn 記事、ジャーナル等、外部取り込みでないと判明している情報
- Ingester の技術構成・実装の説明（リポ説明テーブルでの機能列挙を含む）は、取り込み元サイト名を出さない限り許可

## 著作物・IP 情報の取り扱い

- 本文中で実在作品名・元作品由来の決め台詞・固有口調を使用しない
- 例外: 素材となる Bluesky 投稿に実在作品名・固有名詞が含まれる場合、引用ブロック内のテキストは原文のまま掲載してよい
- 引用を受ける文脈の吹き出しで、引用された固有名詞に言及することも許容する
- 例外: RAG（青空文庫検索）経由で取得した青空文庫作品に限り、本文での言及・引用を許可する（青空文庫はパブリックドメイン作品のみ収録）。作品名・作者名の記載可。RAG 外の作品・翻訳物は対象外
- 全般ルールは `~/.claude/rules/invariants.md`「著作物・IP 情報の混入防止」に準じる
- 本セクションの例外条項を優先適用する

## レビュー観点との対応

本ファイルを参照するレビュースキル:

- `/review-hatena-diary`: 観点「ガイドライン準拠」が本ファイル全節を、観点「物語ガイド遵守」が `narrative-guidelines.md` を参照する
- `/multi-perspective-review`: 観点「ガイドライン準拠チェック」が本ファイル全節を参照する
